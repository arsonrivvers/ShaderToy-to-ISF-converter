# Live smoke — ARShader Milestone 2, phase 3a (panel framework)

**Status: PENDING — legs not yet run on device.**

Phase 3a builds the panel framework: a two-icon rail (Library/Settings), a draggable panel that
opens/closes/swaps, per-section collapse on both decks and MASTER FX with growing monitors as
sections collapse, show mode, and the `⌘⇧P` collapse-everything shortcut. Automated suites are
green; the legs below are reasoned-but-unverified until the operator runs them on device, per the
on-device gate in `CLAUDE.personal.md`.

- **Build under test:** not yet installed (`./scripts/run-instrument.sh` is the operator's call —
  it quits any running ARShader instance and was deliberately NOT run as part of this report).
- **Automated gates:**
  - ARShaderTests: **199 tests, 0 failures** (expected 199 — match)
  - TrueISFEditorTests: **514 tests, 3 skipped, 0 failures** (expected 514 (3 skipped) — match)
  - ShadertoyISFKitTests: **312 tests, 0 failures** (expected 312 — match)
- **Operator:** Conner (unrun)
- **Date:** 2026-07-31
- **Branch:** `m2-panel-framework`, commit `310d397`
- **Result:** PENDING. No leg has been run or signed.

## Legs

State each hypothesis so it can fail. A leg is only CONFIRMED when the operator saw it.

| # | Leg | Hypothesis | Result |
|---|---|---|---|
| 1 | Rail toggle | Rail shows two icons; clicking Library opens the panel, clicking it again closes it entirely | ☐ unrun |
| 2 | Panel swap | Clicking Settings while Library is open swaps without closing | ☐ unrun |
| 3 | Keyboard open/swap | `⌘⌥1` and `⌘⌥2` do the same as clicking | ☐ unrun |
| 4 | Panel resize | Dragging the panel edge resizes it; it does not go below 260pt | ☐ unrun |
| 5 | Independent collapse | Each of SOURCES, FX, PARAMETERS collapses and expands on both decks independently | ☐ unrun |
| 6 | Collapsed summary | A collapsed header still shows its count/summary | ☐ unrun |
| 7 | Monitors grow | **The monitors visibly grow as sections collapse.** (The whole point.) | ☐ unrun |
| 8 | MASTER FX collapse | MASTER FX collapses; its stage count stays visible collapsed | ☐ unrun |
| 9 | Collapse-all shortcut | `⌘⇧P` collapses everything and closes the panel; `⌘⇧P` again restores it exactly | ☐ unrun |
| 10 | Show-mode edit escape | In show mode, expanding DECK A's FX leaves show mode and keeps FX open; the rest stay collapsed | ☐ unrun |
| 11 | Blackout still wins | `⌘B` latches blackout with a panel open, and in show mode. Hold Escape still works | ☐ unrun |
| 12 | Output window toggle | `⌘⇧F` still toggles the output window | ☐ unrun |
| 13 | Mixer strip controls | OUTPUT RES is in Settings and still works. PREVIEW SCALE, CUE SCALE and the OUTPUT destination picker are all still on the mixer strip and still work | ☐ unrun |
| 14 | Arrangement persists | Quit and relaunch: the arrangement is exactly as left | ☐ unrun |
| 14b | Shortcuts fire with field focus | `⌘⇧P` and `⌘⌥1` fire while a text field has focus. Click into the library search field, then the OUTPUT RES width field, and press each shortcut. Blackout uses an app-wide NSEvent monitor precisely because SwiftUI shortcuts are focus-dependent; these new ones do NOT, so this is reasoned-but-unverified until run. If either fails to fire with focus in a field, that is a real defect on stage, not a nicety | ☐ unrun |
| 15 | Generator hides SOURCES | A generator on a deck shows no SOURCES section at all (not an empty one) | ☐ unrun |

## Notes

- This report was generated by running the three regression suites in the foreground and recording
  their verbatim tail output; no install step and no binary-freshness check were performed as part
  of this pass — those are separate steps the operator/controller is handling directly.
- Status stays `PENDING` until the operator runs the legs on device and signs off. Set to
  `CONFIRMED` only on the operator's word, per the on-device gate.
