# SDD ledger — plan: docs/superpowers/plans/2026-08-01-arshader-thumbnails-and-drag-drop.md
Task 1: implementer DONE (commit 30bf31a, 266/266, 3 mutation proofs run) — brief had 2 real bugs, implementer reported and fixed: ISFSceneLoader.load is non-throwing; evict() sorted by access date while touch() wrote modification date
Task 1: review — SPEC ✅, QUALITY FINDINGS 11 (1 Critical, 6 Important, 4 Minor)
Task 1: minor (deferred): M8 queue-isolation assertion is negative-only; add positive === bgCmdQueue check
Task 1: minor (deferred): M9 renderPNG returns without committing the command buffer on the render-failure path
Task 1: minor (deferred): M10 interactiveTask never cleared on normal completion, so cancelInteractive often no-ops
Task 1: minor (deferred): M11 redundant `isValid, let scene` binding (isValid already implies scene != nil)
Task 1: CONTROLLER RULING — C1 originates in the PLAN's own pasted code (the `guard !Task.isCancelled` / store(.unavailable) shape). It contradicts the spec's stated intent that cached failures mean COMPILE failures, so the finding governs over the plan text and the fix is authorised. Recorded rather than silently overridden.
Task 1: fix round 1/5 dispatched (C1, I2, I3, I4, I5, I6, I7) — FIX_BASE 30bf31a
Task 1: fix round 1/5 (7 addressed, 0 open; commits 30bf31a..f19a70f) — re-review VERDICT: ALL ADDRESSED, 268/268 verified independently from the preserved log
Task 1: deferred (Important, carry-over — for final branch review): ISFSceneLoader.load maps BOTH "Could not stage shader source" (filesystem transient) and "Failed to create Metal scene" (GPU/VRAM allocation failure) into !isValid, which render() maps to .shaderFailed and PERSISTS. Under VRAM pressure a 1,500-shader batch sweep could poison a swath of valid entries at once. Same failure class as C1, one layer further down; predates the fix diff.
Task 1: deferred (minor): an unreadable source on a slow/ejected external volume is permanently blacklisted at that mtime (per-spec, but the same shape of problem)
Task 1: deferred (minor): the earlier one-off "123 tests / TEST FAILED" run is undiagnosed, not disproven — log not preserved; two clean full runs since. Host-level abort, no failing case.
Task 1: complete (commits 74a6db8..f19a70f, review clean after 1 fix round)
Task 2: implementer DONE_WITH_CONCERNS (commits 218d17f, 711971c; 272/272) — 3 departures from brief, ALL adjudicated correct by review: deleted testProjectingBelowFullScaleIsFlagged (necessary, it asserted the retired behaviour), diagnosed two literal copies of the conditional, fixed a stale previewScale doc comment
Task 2: review — SPEC ✅, QUALITY FINDINGS 6 (0 Critical, 3 Important, 3 Minor)
Task 2: minor (deferred): previewScale's setter reallocates the master pair even when the resulting size is unchanged — ~16MB of pointless allocation when typing in PREVIEW SCALE mid-show while output is open
Task 2: minor (deferred): opening the projector can produce one malformed frame — masterFX encodes at the stale preview-scaled liveRes into freshly allocated full-size masters. Pre-existing race class (previewScale/outputResolution setters share it), but this task moves the trigger to the instant the projector window appears
Task 2: minor (deferred): isProgramLive mirrors the operator's REQUEST, not whether a window is actually up — OutputPlacement.resolve returns .closed when no screens are attached while destination stays .screen(id:), pinning the chain full-size with nothing projected. Cost-only.
Task 2: CONTROLLER RULING — I1 and I3 both originate in the PLAN's own text (Step 4 mandated two literal copies; Step 8 mandated keeping OutputSharpness as a false-returning "tripwire" that is not coupled to anything). Findings govern over plan text. I1 fixed by extraction rather than comment edit; I3 by deleting the enum + its 3 tautological tests, which will DROP the suite count by 3 deliberately.
Task 2: CONTROLLER RULING — I2 (both scale readouts display false sizes while output is open) is new work the brief did not request, admitted as in scope: it is a direct consequence of the brief's change, the brief touched that block, and it is the only operator-visible statement of the new rule.
Task 2: fix round 1/5 dispatched (I1, I2, I3) — FIX_BASE 711971c
Task 2: fix round 1/5 (3 addressed, 0 open; commits 711971c..18e73d8) — re-review VERDICT: ALL ADDRESSED. I2's partial proof accepted: reviewer independently confirmed no view-introspection capability exists in this project (no ViewInspector/SnapshotTesting anywhere), so the untested wiring is not a skipped-but-achievable test.
Task 2: complete (commits f19a70f..18e73d8, review clean after 1 fix round) — 272/272
Task 3: implementer DONE_WITH_CONCERNS (commit ebd1bc2; 277 with 1 SKIPPED) — review returned SPEC ❌, QUALITY FINDINGS 9 (1 Critical, 5 Important, 3 Minor)
Task 3: minor (deferred): liveDeck lights EVERY slot sharing a shader URL, so two slots holding the same shader with different dialled values both badge live though only one look plays
Task 3: minor (deferred): the live badge depends on ShaderUnit.sourceURL, which this view never observes — correct today only because RenderStatsModel republishes ~2x/sec and incidentally re-evaluates the strip. If InstrumentRenderer's onStats?(nil) paused-loop path ever stops the tick, the badge freezes stale.
Task 3: minor (deferred): the slot number is no longer drawn on the cell (survives only in .help and the a11y label) on a bank the operator fires BY POSITION
Task 3: FINDING OF RECORD — the three PNG surface baselines cover NONE of this task's visible output. SurfaceGeometryTests stubs the slots region as `Color.yellow.frame(height: 40)` and never instantiates SlotBankStripView, so the slot bank ships with zero automated visual coverage. Task 8 leg 23 (idle-vs-live legible on both a near-monochrome and a fully saturated shader) is therefore LOAD-BEARING, not a nicety. Candidate for a real visual-regression suite after the merge.
Task 3: fix round 1/5 dispatched (C1, F2, F3, F4, F5, F6) — FIX_BASE ebd1bc2
Task 3: fix round 1 implementer STALLED waiting on its own background monitor, work complete but UNCOMMITTED. Recovered per the subagent-crash-resume pattern: audited disk state from outside rather than assuming loss — all six fixes present in 4 modified files. Controller ran the suite directly to establish ground truth: 278/278, 0 failures, ZERO skipped (was 277 with 1 skipped), so the disabled gate is genuinely restored. Implementer resumed to commit + write its own fix report; controller did NOT commit its work, which would have bypassed the re-review.
Task 3: fix round 1/5 (6 addressed, 0 open; commits ebd1bc2..aa62863) — re-review VERDICT: ALL ADDRESSED
Task 3: minor (deferred): F5's "no achievable test existed" framing does not hold — ThumbnailService already has a ForTesting-accessor convention (compileCountForTesting, commandQueueForTesting, lastFailureReasonForTesting); a cacheDirectoryForTesting accessor would let a test assert the resolved path is under temporaryDirectory and never applicationSupportDirectory, as a pure string comparison with no I/O. The fix itself is correct.
Task 3: minor (deferred): per-instance UUID temp cache directories are never cleaned up by Instrument — smaller version of the accumulation class F5 closed; self-reported, not hidden
Task 3: FINDING FOR OPERATOR — cells are PINNED at the 96pt floor at every window width (empirically confirmed by a new NSHostingView-backed layout test, not arithmetic). Consequence: the strip does not fill a wide window; dead space to the right of the eight cells on a large display. Real visual trade-off, flagged and NOT fixed.
Task 3: complete (commits 18e73d8..aa62863, review clean after 1 fix round) — 278/278, 0 skipped
SMOKE CHECKPOINT after task 3 (operator, on device, 2026-08-01): legs 1-4 PASS — eight stills fill well, badges/borders/names read correctly, live-vs-idle legible. Operator: "I can see us shrinking this bar a lot" -> strip far too tall, folded into task 4 as Addition 1. Confirmed still missing (correctly, not skipped): RECALL TO A|B, drag-and-drop.
Task 4: implementer DONE (commit f8b7d03; 281/281, 0 skipped) — RECALL TO typed as DeckID (2 segments), SOURCE removed, capture a deliberate no-op until task 6, cells sized by explicit SurfaceMetrics.slotCellHeight = 54pt, knownCellOverflow 233 -> 3
Task 4: CONTROLLER ERROR — my Addition 2 asserted stripsMinWidth's real minimum is "~655". Implementer MEASURED ~619 against the declared 620 and left it unchanged, adding a reality-check test instead. My figure was wrong; the correction stands.
Task 4: STAGED (not proven): the strip-shrink fix rests on .frame(width:height:)'s documented proposal-independent semantics, NOT on empirical proof — mutation testing showed the render harness cannot reproduce the operator's screenshot at all (old buggy code AND new code both measure "pinned" in-harness). Needs an on-device wide-window resize before it is called closed.
OPERATOR DESIGN CORRECTION (2026-08-01, on device): opening the projector made PREVIEW SCALE inert, as task 2 designed. Operator rejects that: preview scale must KEEP operating on the preview screens while the projector runs full-res. My earlier three-way question omitted this option, which was what the operator's ORIGINAL ruling actually described. New rule chosen: preview scale always governs WHAT THE MONITORS SEE — by rendering decks small when output is CLOSED (unchanged, keeps the 99.4->5.8ms saving), and by downscaling a monitor-only copy when it is OPEN (display-bandwidth saving only, NOT shader cost). Becomes a new task.
Task 4: review — SPEC ✅, QUALITY FINDINGS 10 (0 Critical, 4 Important, 6 Minor). Both implementer concerns adjudicated IN ITS FAVOUR: reviewer independently derived the old chain's ~207pt cells / ~116pt rows and confirmed it matches the operator screenshot; the fix mechanism is not in doubt. My "~655" stripsMinWidth figure was wrong and the implementer's measurement stands.
Task 4: minor (deferred): knownCellOverflow = 3 has zero slack — satisfied at exact equality, any 1pt chrome growth fails it
Task 4: minor (deferred): minCellWidth is now a misleading name — it is the cell's exact and only width, not a minimum; slotCellWidth would be truer
Task 4: minor (deferred): stale operator-facing copy — SlotCell.helpText still says "Click to capture the SOURCE deck" and the context menu "Replace with SOURCE deck", both now silent no-ops referring to a removed control. MUST NOT survive task 6. Also currentPreset(of:) and SlotBank.capture(_:into:) have zero production call sites for one cycle.
Task 4: minor (deferred): cell area drops ~4.6x in one step (207x116 -> 96x54) to a size never seen on device; the 9pt name plate now eats ~a quarter of the cell. On-device gate should judge it; slotCellHeight is the single constant to move.
Task 4: minor (deferred): testNoRecallTargetIsAnFXChain is vacuous — LibraryTarget.deck(deck) is .deck by construction, so XCTFail is unreachable for every input. Verbatim from my brief, so spec-compliant.
Task 4: minor (deferred): deckStripsContent widened from private to internal solely for a test
Task 4: fix round 1/5 dispatched (F1 rigid-stub tautology, F2 doc reintroduces the false claim, F3 empty-strip measurement, F4 test reads the operator's real UserDefaults) — FIX_BASE f8b7d03
OPERATOR DECISION (2026-08-01): the revision-4 monitor-only downscale is NOT being built. Controller found it costs more than it saves — one consumer, two passes: the downscale reads all 1920x1080 texels to write a 960x540 copy that a single ~680px tile then samples, versus that tile sampling the big texture directly. Resolution-based preview saving while projecting is ~nil. The real lever is FRAME RATE. Operator chose to pull M3's monitor rate-limit forward into 3c: monitor tiles redraw at ~30fps, program stays at display refresh.
Task 4: fix round 1/5 (F1, F2, F4 addressed; F3 OPEN — commits f8b7d03..36b23d2). Re-review REJECTED F3's resolution.
Task 4: F3 REJECTED IN DETAIL — the fix raised minWindowWidth 1180 -> 1390 off an "815pt measurement". That 815pt is NOT a minimum: the test measures .fixedSize(horizontal:), which requests IDEAL width, and for a Text with no lineLimit ideal width is its full single-line UNWRAPPED width. The 619->815 delta is three pieces of explanatory prose (FXChainView.swift:25 x3, InstrumentView.swift:325, ShaderControlsView.swift:76) that wrap happily in production. Nothing clips at 620.
Task 4: F3 — the criterion refutes itself. stripsMinWidth would become a function of loaded content (empty+expanded 815 -> minWindow 1390; populated 993 -> ~1568; long shader name -> unbounded). And at 1390 with the default 280pt panel open, available is 858 < the 993 populated figure, so it STILL fails by its own standard.
Task 4: F3 — OPERATOR COST, the decisive part: at minWindowWidth 1390 a 13"/14" MacBook at a common "Larger Text" scaled setting (1280-1352 logical) CANNOT FIT THE WINDOW AT ALL. Even a 13" M1 Air at default 1440 leaves 50pt spare — and OutputDestination.floating puts the projector preview in a second window ON THE SAME SCREEN. A test-determinism fix was about to cost the app its hardware compatibility.
Task 4: fix round 2/5 dispatched (revert the 4-constant cascade; retarget-or-retire the ideal-width test; fix a dangling doc reference to a renamed test; correct minWindowWidth's doc) — FIX_BASE 36b23d2
Task 4: fix round 2/5 (F3 addressed; commits 36b23d2..fd2f9f7) — re-review VERDICT: ALL ADDRESSED. Cascade reverted to pre-task-4 values, ideal-width test RETIRED (defensible: the deck strips have no clip-below-a-floor failure mode — Text wraps, Picker compresses, Slider shortens), stripsMinWidth now honestly documented as a hand-chosen usability floor with the full rejected-measurement history, F4's determinism technique preserved in a surviving test, dangling doc ref fixed, minWindowWidth's doc records the tried-and-rejected 1390 and the hardware harm.
Task 4: complete (commits f8b7d03..fd2f9f7, review clean after 2 fix rounds) — 280/280, 0 skipped
OPERATOR RULINGS (2026-08-01): "always pictures never text" — no window size falls back to filenames; the names-fallback breakpoint is STRUCK from the responsive spec. Target hardware is a 16" MBP (1728pt logical) and this is a personal instrument, so the minWindowWidth revert rests on the criterion being wrong, not on 13" compatibility.
Task 4C: implemented (e663888) then fix round 1 (1d290ed). Review returned SPEC ❌ (brief's mutation 2 not run as specified and it SURVIVED both new gates) + 7 findings (0C/4I/3M).
Task 4C: architectural deviation ADJUDICATED CORRECT — a horizontal ScrollView proposes nil on its scroll axis so the brief's literal .frame(minWidth:maxWidth:) cannot clamp; .containerRelativeFrame needs macOS 14 vs this target's 13.0; no feedback loop possible (content column reports the PROPOSAL, and cell width feeds only height); first-render floor fallback correct; chrome arithmetic 127 and all four measured values reconcile exactly.
Task 4C: F1 — retiring the old test left RENDERED-GEOMETRY COVERAGE OF THE SLOT STRIP AT ZERO; the new gates observed computed properties (cellWidth, slotStripRowHeight), not the .frame actually applied to SlotCell. Third consecutive round in this file with the tests-that-cannot-fail failure mode. Fixed; mutation 2 then run as specified and confirmed red.
Task 4C: F2 OPEN TECHNICAL DISAGREEMENT, deliberately not overruled. Reviewer says the "PreferenceKey returns 0 whenever a ScrollView is in the tree" claim is false and the real cause is harness settle count, citing two counter-examples in the same commit. Implementer retested that mechanism directly (restored the PreferenceKey route, raised the settle loop to 60) and it still returned 0. Both investigations recorded; doc comments now state observed behaviour + the tested-and-failed alternative hypothesis rather than a general SwiftUI rule.
Task 4C: F3 RESOLVED by operator — display is 16" MBP at DEFAULT scaling (1728x1117), not More Space. At 1728: cells region 1355, eight cells at the 160 ceiling = 1322, so ~33pt slack — the ceiling is close to an exact fit and the reviewer's ~361pt dead-space concern was contingent on the 2056 reading. ALSO: the ~207pt/~116pt figure for the rejected task-3 bar does NOT reconcile at 1728 (that chain gives ~164) — it was estimated off a screenshot whose pixel width is not logical points. The complaint and the fix stand; only the number was wrong. It appears in the brief, the responsive spec and the task-4 review and should not be re-derived from.
Task 4C: fix rounds 1-2 (4 addressed, 0 open; commits e663888..72fb22e) — re-review VERDICT: ALL ADDRESSED. RenderedCellWidthKey now observes SlotCell's post-frame size (genuine rendered geometry, not a re-derivation); the brief's mutation 2 was run at the real call site and shown red; the reduce-order default-overwrite bug found during diagnosis was eliminated, not papered over; preferenceValue now XCTFails on non-convergence; F3 arithmetic independently reproduced at both 1728 and 2056.
Task 4C: complete (commits ab775f0..72fb22e, review clean after 2 fix rounds) — 281/281, 0 skipped
=== SESSION BOUNDARY 2026-08-01 — handing off. NEXT TASK: 4B (monitor rate-limit), already specced in the plan. Then 5+6 together (drag-and-drop; capture is impossible between them), then 7, then 8 (device session, 45 legs). ===
Task 4B: SKIPPED by OPERATOR DECISION (2026-08-01, second session). Controller read the render path before dispatching and found TWO facts the plan did not have. (1) DEFECT IN THE BRIEF: OutputWindowController.swift:128 registers the PROJECTOR's own view in the same `monitors` NSHashTable, so Step 3's "gate only that loop" would have rate-limited the audience's image and delayed blackout on the wall — the exact two things the task lists as non-negotiable (legs 41, 42). (2) REACH: MonitorView.swift:41-49 shows the PROGRAM tile drivesClock and is deliberately NEVER registered (registering it would recurse draw -> renderFrame -> draw), so it must draw every frame. With the projector excluded too, the divider reaches exactly DECK A and DECK B — two tiles, not three. Combined with InstrumentRenderer.swift:143's own doc ("monitors just sample a texture that already exists, which is nearly free"), the expected saving is ~1.5 fewer ~340px presents per frame and leg 44's REVERT was the likely outcome, not the edge case. Same shape as the revision-4 correction: the operator chose RATE after being told RESOLUTION costs more than it saves. Operator ruled SKIP and go to 5+6. The rate-limit question moves to the existing M3 measurement gate (arshader-m3-perf-measurement-gate-20260801) to be answered with numbers.
Task 4B: consequence — smoke legs 40-44 are STRUCK from task 8. Leg count 45 -> 40 (leg 45, the clamp, survives and should be renumbered or left as-is with 40-44 marked struck; do NOT silently renumber in a way that breaks the 3a/3b leg references).
Tasks 5+6: dispatched as ONE implementer / ONE review cycle (plan requires it — task 4 removed SOURCE, so capture is impossible between them). Implementer DONE (commits 438e103, 4932bdb; 281 -> 292, 0 skipped). Both mutation proofs run red and reverted. Implementer corrected TWO stale brief claims empirically: (a) the PNG surface baselines never needed re-recording — SurfaceGeometryTests.swift:531 hosts Color.gray in the panel slot and never instantiates LibraryPanelView, so Step 9's re-record was premised on coverage that does not exist; (b) the custom UTType belongs in ARShader/Info.plist, not App/project.yml — project.yml:171/177 set INFOPLIST_FILE + GENERATE_INFOPLIST_FILE:NO. Reviewer independently verified both.
Tasks 5+6: review — SPEC ❌, FINDINGS 6 Important / 7 Minor. Reviewer independently confirmed: one and only one bank.capture call site (SlotBankStripView.swift:388); no second modifier; every new API macOS 13.0; 281+11=292 with no test lost; program/PREVIEW SCALE path untouched. Also verified 3 of the 4 report claims I flagged for scrutiny were CORRECT — including that MonitorTile.dragPayload is the genuine production call site (MonitorView.swift:488) and not a parallel reimplementation, which is the tests-that-cannot-fail failure mode this file has now hit four rounds running.
Tasks 5+6: I1 (the decisive one) — the phase's headline invariant has an UNTESTED VIEW SEAM. `accepts` is well covered as a function, but nothing tests that the slot cell CALLS it correctly: mutate `on: .slot` -> `on: .deck(.one)` at SlotBankStripView.swift:377 and all 292 tests stay green while every library drop overwrites any filled slot (accepts returns true unconditionally for a library source on a non-slot destination). Mutating isSlotFilled: is equally green and equally destructive. Compounded by :402-404 re-implementing `!filled || option` inline instead of calling accepts, so highlight and drop derive the invariant from two independent sources — contradicting accepts's own doc comment and the "one place the invariant lives" constraint.
Tasks 5+6: OPERATOR RULING on I3 (plan-mandated) — the brief's row snippet replaced the library row Button with a plain Text, removing the button trait, the activate action, and with click-to-load also gone, EVERY keyboard/VoiceOver path to load a shader in the entire app. Operator ruled KEEP THE ROW AS A BUTTON (drag and tap coexist on .draggable). Controller resolution of the follow-on: the Button's action loads onto DECK A (.deck(.one)), which was InstrumentView's historical libraryTarget default and can never overwrite a saved look. CONSEQUENCE: task 8 leg 35 ("Clicking the library does nothing — a click must not load anything anywhere") is now WRONG and must be rewritten, not deleted; the newer ruling governs.
Tasks 5+6: OPERATOR RULING on I2 (spec miss, Step 7's "highlight must NOT fire for a target that would reject") — unmet for 5 of 6 targets: dragging a deck monitor lights both FX chains, master FX and the other deck. Slots, the only case that can destroy a dialled-in look, ARE correctly filtered. The report called it structurally impossible; the reviewer refuted that (DropDelegate exposes DropInfo.itemProviders in dropEntered/validateDrop, macOS 11+) but every real fix contradicts the brief's explicit "use Transferable, not NSItemProvider" instruction. Operator ruled ACCEPT, document the limitation honestly at both sites (FXChainView.swift:53, MonitorView.swift:509), and add a task-8 leg so it is judged on device rather than argued here.
Tasks 5+6: OPERATOR RULING on I6 — ⌥-click on a filled slot is a permanently silent no-op whose doc comment still claims ⌥ means "the deliberate overwrite". Operator ruled ⌥-CLICK RECALLS, same as a plain click: no gesture on the slot bank silently vanishes. ⌥ keeps its "I mean it" meaning for DROPS onto filled slots, where overwriting is a real risk, and stops meaning anything special for CLICKS, where there is nothing left to overwrite.
Tasks 5+6: minor (deferred): ⌥ is sampled only at hover-ENTER (SlotBankStripView.swift:403), so pressing/releasing ⌥ while already hovering leaves the highlight stale relative to what the drop will do. The drop itself re-reads the flag, so no data-loss path — but the highlight is the operator's only pre-drop signal.
Tasks 5+6: minor (deferred): the shake timer (SlotBankStripView.swift:381-386) is an unstructured, uncancelled Task — two rejections on the same index inside 400ms let the first timer cut the second shake short.
Tasks 5+6: minor (deferred): LibraryTarget.shortLabel + its CaseIterable/Identifiable conformances are production-dead once the picker goes; their only remaining consumer is LibraryPanelTests.swift:80, which now pins a property nothing renders.
Tasks 5+6: minor (deferred): MonitorTile observes elementStats/renderStats but not the deck's ShaderUnit while dragPayload reads unit.sourceURL — so a freshly loaded deck becomes draggable up to ~500ms late, riding the same ~2x/sec republish the task-3 live badge rides. Reviewer checked dismantleNSView and confirmed the if/else identity change costs one bounded viewport rebuild per deck per session, not a leak.
Tasks 5+6: minor (deferred): exported UTI com.arshader.shader-drag is not under the bundle's own com.arsonrivvers.* prefix (Info.plist:104). Plan-mandated; exported UTIs are meant to be domain-owned.
Tasks 5+6: minor (deferred): ShaderDragTests.swift:917 local `url` shadows the instance property at :826 in the same test — compiles, works, reads as a trap.
Tasks 5+6: fix round 1/5 dispatched (I1 extract+prove the slot seam, I2 document the accepted limitation, I3 restore the Button -> deck A, I4 restore .help(entry.name), I5 FXChainView:44 stale copy, I6 ⌥-click recalls) — FIX_BASE 4932bdb
Tasks 5+6: fix round 1/5 (6 addressed, 0 open; commits 4932bdb..aba0283) — re-review VERDICT: ALL ADDRESSED, no new Critical/Important breakage. The NAMED RISK I asked it to check came back safe: wouldHighlight passes a `.library` placeholder source, NOT `.slot` (which accepts rejects unconditionally and would have killed slot highlighting entirely), and the doc comment names `.library, .deck` explicitly rather than overclaiming "every source". Re-reviewer also verified tree-wide that zero live-code references to onCapture / capture(into: / "SOURCE deck" survive.
Tasks 5+6: minor (deferred): the implementer's "no achievable test" claim for F3-F6 is correct for F5 (a constant string) and F6 (the fix REMOVED the only branch worth testing), but NOT for F3 and F4. F3's Button action encodes a real decision — "always deck A, never configurable" — and F4's tooltip assembles two strings; both are extractable as pure functions exactly the way F1 and task 6's dragPayload were extracted IN THIS SAME ROUND. Cheaper coverage was achievable than claimed. Does not reopen the findings; flagged for the final branch review because it is the fifth appearance of this class in this file.
Tasks 5+6: complete (commits 4f13981..aba0283, review clean after 1 fix round) — 297/297, 0 skipped
Tasks 5+6: CONTROLLER ERROR — I edited the plan file (smoke legs 34b/35/35b) while the fix-round implementer was live, and it committed my in-flight edit inside aba0283. It staged by explicit path and DISCLOSED the foreign change in its commit message rather than passing it off, so no damage and correct attribution. My fault: do not edit tracked files while an implementer is running, even files outside its brief.
Task 7: implementer DONE_WITH_CONCERNS (commit 3edc396; 298/298, 0 skipped). Brief had TWO defects I caught pre-dispatch (fixtureURL("slow") names a fixture that does not exist; fixtureURL is `throws` and the brief's snippets omit `try`, so they do not compile). Implementer then proved the brief's testAnInteractiveRequestSupersedesItsPredecessor UNDELIVERABLE — 24 trials, 0 wins — and shipped no test rather than a flaky one, which is the pre-authorised acceptable outcome.
Task 7: FINDING OF RECORD — PRODUCTION SUPERSESSION DOES NOT HOLD. Reviewer independently verified the mechanism in ThumbnailService.swift: render(_:) (:150-165) has NO await between entry and either return (String(contentsOf:) :153, ISFSceneLoader.load :156, renderPNG :161, which blocks on DispatchSemaphore.wait :201). Swift actors run isolated jobs to completion without preemption, and cancelInteractive() (:141-144) is itself actor-isolated, so it cannot begin until the actor is free. Consequence: hover B's `interactiveTask?.cancel()` at :100 ALWAYS fires after render A already completed and is ALWAYS a no-op. A pointer sweep across N uncached rows performs N FULL renders, serialized, and keeps going after the pointer stops — each an ISF compile + offscreen render + readback + PNG encode. The disk cache bounds it to once per shader and the work is on bgCmdQueue not the live queue, but the GPU and the shader compiler are shared. This is EXACTLY the load task 8 leg 28 exists to test. Second operator-visible symptom: the well shows the PREVIOUS shader's still with no loading state until the backlog drains, so during a sweep the preview can be seconds behind the pointer while looking like a settled answer. The task SURFACED this defect rather than causing it — the .interactive priority never worked as designed, since task 1.
Task 7: CONTROLLER RULING — the dwell delay is IN SCOPE for this task's fix round. It is ~2 view-side lines (`.task(id: hoveredURL)` sleeps before requesting; SwiftUI cancels the previous .task instance on every id change, so rows merely swept past never issue a request at all), it needs no ThumbnailService change, and it moves supersession UPSTREAM of the actor where cancellation actually lands. Leaving it would send the operator into task 8's device session to run leg 28 against a shed that is known not to shed. 150ms is a starting number, trivially changed.
Task 7: review — SPEC ❌, FINDINGS 3 Important / 6 Minor. Reviewer confirmed: no test lost (297->298), no production surface added for test convenience, ThumbnailService untouched, all three of my ambiguity resolutions implemented exactly, mutation evidence internally consistent and corroborating a real run (quoted RED text matches the shipped test byte-for-byte).
Task 7: I3 — Step 4's mutation was SUBSTITUTED, not run as specified. The brief names "make cancelInteractive() cancel everything"; what ran was a STICKY flag poisoning every future render. The brief's literal mutation would NOT fail the shipped test, because at cancel time no batch work exists (ThumbnailServiceTests.swift:128-130). The shipped test is testAValidShaderProducesAnImage (:39-43) plus a no-op call. It CAN fail (the sticky mutation goes red) so it is not a cannot-fail test, but its power is confined to that one bug class. Reviewer's structural point, which I accept: `.batch` never registers anywhere cancelInteractive() reads (:97-98 vs :141-144), so "cancelling hover drops a queued bank thumbnail" has NO REACHABLE FAILURE MODE under actor serialization — the original guarantee is unfalsifiable as the service is built, and the honest resolution is to say so in the plan rather than keep a near-duplicate test standing in for it.
Task 7: minor (deferred): hover-exit cancelInteractive() is a no-op in essentially every real case — in-flight renders cannot be interrupted, a completed interactiveTask is never cleared on normal completion so .cancel() does nothing, and it cannot touch pending thumbnail jobs. Leaving the parked interactiveTask finding unfixed is SAFE (cancelling a finished task is harmless), so that earlier ruling stands; but the exit call is brief-mandated cost with no delivered benefit.
Task 7: minor (deferred): no loading or stale state in the well — while a request is outstanding it keeps the previous shader's still with no indication. Largely mitigated by the dwell delay (requests only fire on rest), but a dim-while-outstanding would close the honesty gap for free at fixed height.
Task 7: minor (deferred): accessibilityIdentifier("libraryPanel.hoverPreview") has no consumer — this suite is unit tests, not XCUITest, and nothing references it. Reads as UI-test coverage that does not exist. Also no accessibilityLabel on the well's image, so VoiceOver reads nothing.
Task 7: minor (deferred): LibraryPanelView grew ~74 lines (~+65%). Still cohesive, but hoverPreviewWell plus the decode/.task wiring would extract cleanly into a HoverPreviewWell view, which would also make the decode path testable.
Task 7: minor (deferred, CANNOT VERIFY FROM DIFF — for the device session): whether .onHover on a macOS List (NSTableView-backed) fires exit only at the whole-list boundary or also on row-to-row transit. If per-gap, hover-exit cancel runs constantly mid-sweep. Also unverified: the unconditional 120pt well + padding at the panel's real size, which is the "ideal width is not minimum width" class of question.
Task 7: fix round 1/5 dispatched (F1 false on-device claim in shipped source, F2 three comments asserting a disproved guarantee, F3 record the substituted mutation honestly + fix the misleading failure message, F4 the dwell delay) — FIX_BASE 3edc396
Task 7: fix round 1/5 (4 addressed, 0 open; commits 3edc396..9b21ad4) — re-review VERDICT: ALL ADDRESSED, no new breakage. The NAMED TRAP came back clean and it is the first time this phase an extraction did not need a second look: waitOutHoverDwell uses a real do/catch (LibraryPanelView.swift:48-55), NOT `try?` which would have swallowed the cancellation and made the whole dwell a silent no-op; the production .task(id:) closure calls it at :190 and the test calls THE SAME top-level function at LibraryPanelTests.swift:117 — not a reimplementation, not an unreached extraction. Re-reviewer also reasoned the mutation through independently (the sticky `_ = try? await Task.sleep; return .dwelled` variant forces .dwelled unconditionally, so asserting .cancelledEarly fails on mechanism, not timing) and confirmed the reported 0.001s-vs-0.314s timings rule out a slow-machine coincidence.
Task 7: complete (commits aba0283..9b21ad4, review clean after 1 fix round) — 299/299, 0 skipped
CONTROLLER RULING — ORDER SWAPPED: the final whole-branch review runs BEFORE task 8, not after as the plan lists. Reason: task 8 installs the app and hands the operator 40 smoke legs. If the final review lands a fix wave afterwards, the build he signed is not the build that merges — which is the exact stale-diff attribution problem that folding 3a/3b's legs into task 8 was meant to solve. Final review first, fix wave, THEN regression + install + device session against the code that actually merges.
=== SESSION BOUNDARY 2026-08-02 — final whole-branch review, two fix waves, task 8. ===
Final whole-branch review (60 commits, 02fbcd4..9b21ad4): VERDICT **MERGE AFTER FIX WAVE** — 10 findings, 0 Critical. Branch judged internally coherent. Headline: F1 SlotBankStore's all-or-nothing decode could lose the whole bank permanently, and the codebase had ALREADY solved that class twice one file over (Arrangement's hand-written init(from:), ParamSnapshot's FailableParamValue) — the store holding the only irreplaceable data got neither.
Review CORRECTED THE LEDGER on the supersession mechanism (line 87 was wrong): it is NOT "the actor is busy so cancelInteractive() cannot start" — the actor IS free during hover A's `await task.value`. It is an EXECUTOR FIFO race: render A's job is enqueued strictly before hover B can enqueue its cancel. Matches task 7's 24-trials-0-wins exactly and explains why fixture duration never moved it. CONSEQUENCE: no generation counter or pendingInteractiveURL scheme can EVER shed it — every such fix loses the same race; only a real suspension point inside render() works. Action item updated; the cheap fixes are now explicitly fenced off.
Review SHRANK the ISFSceneLoader finding (line 11): load takes SOURCE TEXT, not a URL, so the "ejected external volume" half does not exist, and nothing in this branch sweeps the library, so mass-poisoning is unreachable. Blast radius is thumbnails only. Filed as a ~2-line ThumbnailCache expiry, NOT a change to the editor-shared loader.
OPERATOR RULING (2026-08-02) — LIBRARY CLICK IS INERT AGAIN, reversing the 2026-08-01 ruling at line 72. Operator waived the keyboard/VoiceOver argument outright: personal, mouse-and-MIDI-driven instrument, no non-pointer path wanted. Drag is the ONLY pointer path to load. Leg 35 reverted to its ORIGINAL assertion (second rewrite); leg 35b STRUCK in place, not deleted, not renumbered.
OPERATOR RULING (2026-08-02) — Clear gets ONE-DEEP UNDO plus an "Undo clear" context-menu item. Explicitly NOT a confirmation dialog: the strip is a live performance surface and a modal mid-set is the thing being avoided. Review had proposed the model half with no invocation path; an undo nothing can invoke is dead code.
OPERATOR RULING (2026-08-02) — the ISFSceneLoader ensureGlobals race was ruled IN SCOPE despite the reviewer excluding it (shared with the TrueISFEditor target). The implementer then REFUSED IT CORRECTLY: no race exists.
Fix wave (14 items, 9b21ad4..7fff542): 299 -> 325 tests, 0 skipped. TrueISFEditor untouched at 514. Three brief defects caught by the implementer, all correct: F1's compactMap would have SHIFTED every later slot left (used map); F6's ordered assertion CANNOT FAIL (.frame(width:height:) applied outside SlotCell reports its own size regardless of the child — proven by mutating with .padding and staying green), deleted rather than ship a seventh cannot-fail test; F4's ordered outcome unavailable — every path that moves the live size has already replaced masters with undefined contents, so it clears to BLACK and skips rather than showing the previous master.
FINDING OF RECORD (2026-08-02) — ensureGlobals IS DEAD CODE IN BOTH TARGETS. ISFMSLCache.h is under NS_ASSUME_NONNULL_BEGIN so Swift imports the statics as non-optional; the compiler already warns at ISFSceneLoader.swift:26-27 that both `if ... == nil` bodies are statically unreachable. Re-review then found VVMTLPool.h:21/38/119 has the IDENTICAL pattern, so InstrumentRenderer.swift:201's supposed "pre-set" is the same dead guard. Exhaustive grep, no truncation: ISFMSLCache.primary assigned at 3 sites, VVMTLPool.global at 4, ALL 7 behind statically-false guards. NEITHER global is ever installed. Consequence: every FIRST sight of a shader pays a full transpile + Metal compile — a named hypothesis for smoke leg 28, not a mystery. Filed (arshader-ensureglobals-dead-msl-cache-20260802); deliberately NOT fixed, since making the check live would install both globals for the first time in two shipping targets.
Scoped re-review (9b21ad4..7fff542): VERDICT **MERGE AFTER 1 FIX**. Confirmed 13/14 landed. Found F7's flag lifecycle broken — isResolvingPreview was cleared by a SUPERSEDED hover task while a newer request was still outstanding, reproducing the exact defect F7 was landed to fix, and ACTIVELY WORSE than pre-F7 (the indicator appears then vanishes, reading as "resolved" over a stale still). Also answered the question the fix-wave brief asked: yes, loadFailed could stick true permanently and silently — a future non-optional Preset field would give 40 empty slots AND permanently refused saves, recoverable only via `defaults delete`.
CONTROLLER RULING (2026-08-02) — F1's escape valve OVERRIDES the re-review's suggested `.recovered` side key: on decode failure, back the raw unreadable bytes up under a DATED key, then allow saves normally. Strictly dominates — nothing is lost, nothing is refused, and there is no lockout state to escape from. No UI built; the operator-visible signal is filed separately.
Fix round 2 (7fff542..e318f0a): 325 -> 327 tests, 0 skipped. DEVIATION, verified correct by the controller: item 1's generation guard could not be a plain @State var — @State storage only functions inside a LIVE SwiftUI view graph, so writes on a directly-constructed view are silently discarded, which made the flag lifecycle (the one part of F7 most needing a test) structurally unreachable. Moved to `HoverPreviewResolver: ObservableObject` via @StateObject, following LibrarySelection's existing pattern in the same file. Controller traced the guard against all four lifecycle paths (superseded/normal/list-exit/swept-past); no path leaves the flag stuck true, which was the named opposite-failure risk. F1's two pre-existing tests were rewritten to assert the new backup-then-resume contract — strengthened, not weakened: the new one round-trips through a FRESH store rather than an in-memory read.
LEG ACCOUNTING CORRECTED (2026-08-02) — the handoff's "40 + 34b + 35b" double-counted. 34b is INSIDE the 40; 35b is struck. And phase 3b's legs are NOT additive: 3c's list reuses 3b's numbers and titles verbatim for 1-16, 18, 19 (verified title-for-title), skipping 17 because it was signed 2026-07-31. So 3c SUBSUMES 3b by construction and "fold in 3b's remaining 18" would have DUPLICATED 18 legs. Final count: **44** = 40 surviving + 4 new (46 undo, 47 projector black frame, 48 hover well never lies, 49 slot number by position). Legs 6 and 7 were already re-scoped in the plan; no work needed.
Task 8: report written as TWO TIERS (Tier 1 = new/destructive/fix-wave-touched, enough to sign the merge; Tier 2 = regression sweep) so the device session can be split across two sittings without the controller pre-deciding.
NOTE — scripts/run-instrument.sh hardcodes DDATA=/tmp/arshader-ddata, the FORBIDDEN path while codex sessions are live in the main checkout. Do NOT run it from this worktree; build and ditto by hand against /tmp/arshader-ddata-bank.
TASK 8 — suites run, report written, build installed and launched. HEAD 1839027, 66 commits ahead. ARShader 327 (326 pass + the 1 known pre-existing hang), TrueISFEditor SUCCEEDED, ShadertoyISFKit 312/0. Installed dylib verified byte-identical to the build product (sha256 dd796e22) and carrying 3 string literals unique to this session. All 44 legs UNRUN — the branch is STAGED, nothing is CONFIRMED.
FREEZE INVESTIGATION (operator-requested mid-session, systematic-debugging). HEADLINE: the ARShader suite is a 25-SECOND suite. Every multi-minute "the tests are frozen" run was ONE test — testSteadyStateAllocatesNoNewTextures, the only test that renders in a LOOP (31x renderFrame) — hanging and dying with SIGTERM. PRE-EXISTING since 67048d9 (Milestone 1); same signature as this file's line-13 undiagnosed "123 tests / TEST FAILED" from task 1, which is now explained in kind if not in cause.
FREEZE — THE TRAP THAT HID IT: after a host restart xcodebuild's CONSOLE prints a PER-LAUNCH summary ("Executed 175 tests, with 0 failures") for a run that FAILED with a real total of 327. A run can print "0 failures" and be a failed, partial-looking run. I misread it that way myself mid-session and had to correct it. ONLY `xcrun xcresulttool get test-results summary --path <bundle>.xcresult` tells the truth.
FREEZE — FIXED (mitigation, proven): the 31-frame loop now runs on a background queue behind a 30s XCTestExpectation wait (house idiom from BlackoutTests/FXChainTests/DeckTests). Mutation-proved: injected sleep -> failed at 30.128s with a named message; reverted -> 0.005s. A hang is now a fast named failure inside an accurate 327/326/1 run instead of a silent 195-518s stall. The `first === last` pooled-texture assertion is preserved BYTE-FOR-BYTE — it is a real GPU-leak guard on the live render path and must never be deleted to make the suite green.
FREEZE — ROOT CAUSE STILL OPEN. Ruled out with evidence: the test alone (0.003s), its class alone (7/7), the 12 preceding classes (153/153), parallel execution (parallelizable="NO"), a concurrent session (reproduced on a quiet machine), two concurrent test hosts (tested directly, both succeeded), Instrument's sweepCache Task (already gated on TestHarness.isActive), and THE TEST HOST'S WINDOW.
FREEZE — REFUTED HYPOTHESIS, recorded because it was compelling and wrong: the operator reported it "hangs immediately when I click into the window", and ProgramView/MonitorView are free-running MTKViews (60fps, enableSetNeedsDisplay=false) whose draw(in:) runs on the CVDisplayLink thread into the lock-guarded InstrumentRenderer — a perfect story for colliding with the one looping test. I gated ARShaderApp's WindowGroup content on TestHarness.isActive so the harness built NO InstrumentView at all. THE HANG REPRODUCED ANYWAY. Hypothesis dead; the entry-point change was REVERTED rather than shipped on a disproven theory. Untested successor lead: all 3 observed hangs followed a REBUILD while ~6 clean runs were warm.
FREEZE — REAL BUG FOUND ON THE WAY, filed separately: TestHarness.swift's doc comment claims "window focus-stealing" gates on the flag, but ARShaderApp's WindowGroup is NOT gated, so the full instrument UI including 60fps MTKViews genuinely runs during every suite. Not the hang cause, but real GPU waste and focus-stealing. Deliberately NOT bundled into this merge.
OPEN AT SESSION END: the 44 legs; and MERGE COORDINATION — a concurrent Claude session is working the ARShader modulation slice (ModSource.swift, /tmp/arshader-ddata-mod) in another checkout. Merging 66 commits under it is a timing question for the operator, not a technical one.
